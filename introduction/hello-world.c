// program to print "hello world" to the console

// the following statement is a preprocessor directive
// preprocessor: converts - source code -> expanded source code
// compiler: converts - expanded source code -> machine code
#include <stdio.h>

int main()
{
    printf("hello world\n");
    return 0;
}